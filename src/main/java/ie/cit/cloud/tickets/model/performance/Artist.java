package ie.cit.cloud.tickets.model.performance;

public class Artist extends Performer
{
	public Artist(final String performerId, final String performerName)
	{
		super(performerId, performerName);
	}
}
